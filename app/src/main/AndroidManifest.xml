<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="alc4phase1.gpf_zw">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/andela_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/andela_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        >
        <activity
            android:name="alc4phase1.gpf_zw.ProfileActivity"
            android:label="@string/my_profile"
            android:parentActivityName="alc4phase1.gpf_zw.MainActivity"
            />
        <activity
            android:name="alc4phase1.gpf_zw.AboutActivity"
            android:label="@string/about_alc"
            android:parentActivityName="alc4phase1.gpf_zw.MainActivity"
            />
        <activity android:name="alc4phase1.gpf_zw.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>